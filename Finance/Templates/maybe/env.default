# openssl rand -hex 64 OR head -c 64 /dev/urandom | od -An -tx1 | tr -d ' \n' && echo
SECRET_KEY_BASE="replacemewiththegeneratedstringfromthepriorstep"
POSTGRES_PASSWORD="replacemewithyourdesireddatabasepassword"
