# Lowering privileged ports to 53 to allow us to run rootless Podman containers on lower ports
# Actual file at: `/etc/sysctl.d/podman-privileged-ports.conf`
# From: www.smarthomebeginner.com
# default: 1024

net.ipv4.ip_unprivileged_port_start=53
